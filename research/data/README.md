# Data policy

Do not commit large, generated, private, or redistributable data by default.
Keep small, stable test fixtures in `tests/test_data/` instead. Document data
sources, licenses, transformations, and regeneration steps with the research
that uses them.
